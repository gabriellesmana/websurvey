// script.js
document.addEventListener("DOMContentLoaded", function () {
    // Hapus elemen loading setelah halaman selesai dimuat
    var loadingOverlay = document.getElementById('loading-overlay');
    loadingOverlay.style.display = 'none';
});
